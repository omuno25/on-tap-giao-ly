# Đóng góp

Cảm ơn bạn muốn cải thiện dự án.

## Quy trình

1. Tạo issue mô tả lỗi hoặc đề xuất nếu thay đổi có phạm vi lớn.
2. Fork repository và tạo branch riêng.
3. Cài dependency bằng `pnpm install --frozen-lockfile`.
4. Thực hiện thay đổi nhỏ, tập trung và có mô tả rõ ràng.
5. Chạy `pnpm check` và `pnpm audit` trước khi gửi pull request.

## Nội dung và audio

Chỉ gửi dữ liệu, bài viết hoặc audio mà bạn có quyền phân phối theo `CONTENT_LICENSE.md`. Pull request phải nêu nguồn nội dung và, với audio AI, tên dịch vụ hoặc model đã dùng cùng xác nhận rằng điều khoản cho phép phân phối đầu ra.

Không gửi API key, dữ liệu cá nhân, bản ghi giọng nói được clone khi chưa có sự đồng ý, hoặc tài liệu không rõ quyền sử dụng.

Bằng việc gửi đóng góp, bạn đồng ý cấp phép mã nguồn theo MIT và nội dung đủ điều kiện theo CC BY 4.0 như mô tả trong repository.
